# Dashboard route structure

This folder uses role-based route groups:
- (supporter): supporter-specific dashboard views
- (creator): creator-specific dashboard views
- (admin): admin-specific dashboard views

Each role group can contain its own nested routes for navigation, analytics, and management features.
